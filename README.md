# amrojsandhu.github.io

## Build

```shell script
bundle exec jekyll server --watch  --incremental
```

## Test

Add the following to _config.yml

```yaml
# Filtering Content
future              : true
unpublished         : true
```